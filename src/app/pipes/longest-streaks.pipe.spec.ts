import { LongestStreaksPipe } from './longest-streaks.pipe';

describe('LongestStreaksPipe', () => {
  it('create an instance', () => {
    const pipe = new LongestStreaksPipe();
    expect(pipe).toBeTruthy();
  });
});
