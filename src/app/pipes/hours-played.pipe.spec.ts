import { HoursPlayedPipe } from './hours-played.pipe';

describe('HoursPlayedPipe', () => {
  it('create an instance', () => {
    const pipe = new HoursPlayedPipe();
    expect(pipe).toBeTruthy();
  });
});
