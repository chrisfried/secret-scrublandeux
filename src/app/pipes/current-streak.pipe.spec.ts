import { CurrentStreakPipe } from './current-streak.pipe';

describe('CurrentStreakPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrentStreakPipe();
    expect(pipe).toBeTruthy();
  });
});
