name: Build and Deploy
on:
  push:
    branches:
      - master
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v2

      - name: Install and Build 🔧
        run: |
          npm install
          npm run build:ghp
        env:
          BAK: '25cec645b08c40a393deccc37749b2c6'
          BAU: 'https://www.bungie.net/en/OAuth/Authorize'
          BCI: '30141'
          BCS: 'Qh0txds-DSzcPNS6kl6RVBFqtMxVkQK7Cs40wbB3C78'
          BR: 'https://chrisfried.github.io/secret-scrublandeux'

      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@4.1.0
        with:
          branch: gh-pages
          folder: dist
