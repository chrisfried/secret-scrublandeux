import { ActivitiesTotalHoursPipe } from './activities-total-hours.pipe';

describe('ActivitiesTotalHoursPipe', () => {
  it('create an instance', () => {
    const pipe = new ActivitiesTotalHoursPipe();
    expect(pipe).toBeTruthy();
  });
});
