import { ActivitiesTotalTimePipe } from './activities-total-time.pipe';

describe('ActivitiesTotalTimePipe', () => {
  it('create an instance', () => {
    const pipe = new ActivitiesTotalTimePipe();
    expect(pipe).toBeTruthy();
  });
});
