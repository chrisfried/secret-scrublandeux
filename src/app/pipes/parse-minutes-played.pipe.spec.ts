import { ParseMinutesPlayedPipe } from './parse-minutes-played.pipe';

describe('ParseMinutesPlayedPipe', () => {
  it('create an instance', () => {
    const pipe = new ParseMinutesPlayedPipe();
    expect(pipe).toBeTruthy();
  });
});
