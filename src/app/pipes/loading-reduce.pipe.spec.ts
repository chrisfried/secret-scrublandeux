import { LoadingReducePipe } from './loading-reduce.pipe';

describe('LoadingReducePipe', () => {
  it('create an instance', () => {
    const pipe = new LoadingReducePipe();
    expect(pipe).toBeTruthy();
  });
});
