import { DestinyHashPipe } from './destiny-hash.pipe';

describe('DestinyHashPipe', () => {
  it('create an instance', () => {
    const pipe = new DestinyHashPipe();
    expect(pipe).toBeTruthy();
  });
});
